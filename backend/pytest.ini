[pytest]
# Tests import `backend.*` and `api.*` via absolute paths, so the repo root must be
# importable. Without this, running `pytest` from inside backend/ fails collection
# with ModuleNotFoundError: No module named 'backend'.
pythonpath = . ..
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
    ignore::RuntimeWarning
