# Development / CI dependencies.
#
# These are NOT installed in production. requirements.txt is the runtime set;
# this file adds the test runner. Install both for local work:
#
#   pip install -r requirements.txt -r dev-requirements.txt
#
# Why this file exists: pytest was previously absent from every dependency
# manifest, so `python -m pytest` could only ever run on a machine where
# someone had pip-installed it by hand. That is why the backend had no CI
# coverage at all. The backend-tests job installs this file.

pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
